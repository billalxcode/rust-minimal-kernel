# Workspace-wide default settings
[build]
target = "i386-unknown-none"

[unstable]
build-std = ["core", "compiler_builtins"]
build-std-features = ["compiler-builtins-mem"]

[target.i386-unknown-none]
rustflags = [
  "-C",
  "relocation-model=static",
  "-C",
  "link-arg=-Tkernel/linker.ld",
]