[package]
name = "bootloader"
version = "0.1.0"
edition = "2021"
authors = ["Billal Fauzan"]
license = "MIT"
description = "Custom bootloader for Rust OS"

[lib]
name = "bootloader"
path = "src/lib.rs"

[dependencies]
osdev-utils = { path = "../lib/osdev-utils" }